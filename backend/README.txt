Wings Cafe Inventory - Full Project

Backend:
  cd backend
  npm install
  npm start
  -> server runs on http://localhost:5000

Frontend:
  cd frontend
  npm install
  npm start
  -> app runs on http://localhost:3000

Images are in frontend/public/images (SVG placeholders). Replace with real images if desired.
